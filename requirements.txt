# Universal Video Downloader — Python dependencies
# Install: pip install -r requirements.txt
# Recommended: pip install -U "yt-dlp[default,curl-cffi]"

yt-dlp[default,curl-cffi]>=2026.8.19
requests>=2.32.2
# Threads downloads (download_threads_video) — yt-dlp has no Threads extractor
selenium>=4.16.0

# Dev / build (optional)
# pyinstaller>=6.0
# pillow>=10.0
